import {SSC} from "ssc.slint";
import {Interface} from "interface.slint";

export struct DeviceModel {
    name: string,
    ssc: SSC,
    interface: Interface,
    serial: string,
    firmware: string,
    path: string,
}