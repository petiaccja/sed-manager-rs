import { Status } from "status.slint";

export struct Device {
    status: Status,
    name: string,
    serial: string,
    path: string,
    firmware: string,
    interface: string,
    security_commands: bool,
    is_removable: bool,
}
